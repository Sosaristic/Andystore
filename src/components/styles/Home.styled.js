import styled from "styled-components";

export const HomeStyled = styled.div`
position: relative;
margin: .5rem 0;

.filter{
    display: flex;
}
span{
    margin-left: auto;
    padding: .5rem;
    font-size: 1rem;
}
#filter-option, option{
    
    text-align: center;
    outline: none;
}
`